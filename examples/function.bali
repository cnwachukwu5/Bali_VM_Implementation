var a = 10;
def main(x) {
    var b = 10;
    var i = 0;

    print x - a - b;
    if (b == 10) {
        print x;
    };

    while (a > i) {
        print a * i;
        i = i + 1;
    };
    return 0;
}

main(100);